import { createContext, useEffect, useState } from 'react';import { getLiveEvent } from '../services/event.service';export const EventContext=createContext(null);export function EventProvider({children}){const [event,setEvent]=useState(null);useEffect(()=>{getLiveEvent().then(setEvent);},[]);return <EventContext.Provider value={{event,setEvent}}>{children}</EventContext.Provider>;}
