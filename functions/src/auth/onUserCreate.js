import { onUserCreated } from 'firebase-functions/v2/identity';import admin from 'firebase-admin';if(!admin.apps.length) admin.initializeApp();export const onUserCreate=onUserCreated(async(event)=>{await admin.firestore().collection('users').doc(event.data.uid).set({name:event.data.displayName||'',email:event.data.email||'',phone:event.data.phoneNumber||'',role:'attendee',language:'en',loyaltyPoints:0,createdAt:admin.firestore.FieldValue.serverTimestamp()},{merge:true});});
